{
  "comment": "This template is deprecated. Use YAML configuration files with CSV manifests instead.",
  "comment2": "See examples/batch_params_azure_example.yaml for the recommended approach.",
  "comment3": "Task lists should be provided via CSV manifest files, not in config files.",
  
  "deprecated_example": {
    "defaults": {
      "prefilter_model_type": "CTRANSPATH",
      "segment_threshold": 0,
      "patch_size": 256,
      "mpp": 0.5,
      "num_workers": 4,
      "batch_size": 64,
      "use_gpu": true,
      "keep_intermediate_files": false
    },
    "tasks": "Use CSV manifest files for task lists instead"
  }
}
