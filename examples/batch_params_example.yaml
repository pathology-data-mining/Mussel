# Example: parameters configuration for use with CSV manifest
# Use this file with --config-file-params when specifying --csv-manifest
# The CSV provides slide IDs and paths, this file provides all the parameters

# Processing parameters
prefilter_model_type: CTRANSPATH
batch_size: 64
num_workers: 4
use_gpu: true
classifier_threshold: 0.75

# Segmentation configuration
seg_config:
  # Option 1: Use a preset group (biopsy, resection, tcga, or default)
  # group: biopsy
  
  # Option 2: Specify individual parameters (overrides group if both specified)
  patch_size: 256
  mpp: 0.5
  segment_threshold: 0
  # step_size: 256  # Optional: defaults to patch_size
  # seg_level: -1  # Optional: -1 for auto
  # segment_max_value: 255
  # median_blur_ksize: 7
  # morphology_ex_kernel: 0
  # ref_patch_size: 512
  # use_otsu: false
  # tissue_area_threshold: 100
  # hole_area_threshold: 16
  # max_num_holes: 8

# Aggregation settings
aggregation_method: identity

# Optional: Postfilter model
# postfilter_model_type: VIRCHOW

# Optional: Slide-level aggregation
# aggregation_method: model
# slide_model_type: GIGAPATH_SLIDE
# slide_batch_size: 8

# Retry configuration
max_retry_count: 3

# Note: Sensitive fields like AWS credentials should be provided via command-line
# not in the config file for security
